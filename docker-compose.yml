services:
  localstack:
    image: localstack/localstack:3.0.2
    ports:
      - "4566:4566"
    environment:
      - SERVICES=sqs
    volumes:
      - "localstack-data:/var/lib/localstack"

volumes:
  localstack-data:
